#include <stdio.h>

int main() {
    char myChar = '.';  // The character to compare

    if (myChar == '.') {
        printf("The character is a dot.\n");
    } else {
        printf("The character is not a dot.\n");
    }

    return 0;
}